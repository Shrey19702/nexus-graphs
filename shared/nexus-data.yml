# Optional shared defaults (fallback image cache-bust).
# Dataset paths and per-hub image folders live in shared/js/corpora.js.
# Per-hub PNG maps: images/<corpus>/theme-images.json

images_version: "1787825571792"
